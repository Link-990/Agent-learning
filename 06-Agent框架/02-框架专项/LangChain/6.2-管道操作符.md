# 6.2 管道操作符 | 的使用

`|` 是 LCEL 的核心——把组件串成链。

---

## 基本用法

```python
from langchain_core.prompts import ChatPromptTemplate
from langchain_openai import ChatOpenAI
from langchain_core.output_parsers import StrOutputParser

prompt = ChatPromptTemplate.from_template("翻译：{text}")
model = ChatOpenAI(model="gpt-4o-mini")
parser = StrOutputParser()

chain = prompt | model | parser
result = chain.invoke({"text": "Hello, world!"})
print(result)  # "你好，世界！"
```

---

## 数据流

```
输入 {"text": "Hello"} 
    ↓ prompt
ChatPromptValue 
    ↓ model  
AIMessage(content="你好")
    ↓ parser
"你好"（字符串）
```

---

## 链的属性

所有 LCEL 链都支持：

```python
chain.invoke(input)      # 同步调用
chain.stream(input)      # 流式调用
chain.batch([input1, input2])  # 批量调用
await chain.ainvoke(input)     # 异步调用
```

---

## 小结

- `|` 把组件串成链
- 数据从左到右流经每个组件
- 链支持 invoke/stream/batch/ainvoke

---

👉 下一节：[6.3 Runnable 接口](6.3-Runnable接口.md)
