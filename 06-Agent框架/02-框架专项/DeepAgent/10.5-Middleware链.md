# 10.5 Middleware 链

Kami Code 的 `DeferredToolMiddleware` 是 Deep Agents Middleware 系统的经典应用。

---

## 核心代码

```python
class DeferredToolMiddleware(AgentMiddleware):
    def __init__(self, activation_state):
        self.activation_state = activation_state
    
    def visible_tools(self, tools):
        visible = []
        for tool in tools:
            metadata = getattr(tool, "metadata", {})
            
            if not metadata.get("defer_loading", False):
                visible.append(tool)
                continue
            
            public_name = metadata.get("public_name")
            if self.activation_state.is_active(public_name):
                visible.append(tool)
        
        return visible
    
    def wrap_model_call(self, request, handler):
        filtered = self.visible_tools(request.tools)
        return handler(request.override(tools=filtered))
```

---

## 工作原理

```
LLM 调用前
    ↓
DeferredToolMiddleware.wrap_model_call
    ↓
visible_tools() 过滤：
    - defer_loading=False → 保留
    - defer_loading=True 且已激活 → 保留
    - defer_loading=True 且未激活 → 移除
    ↓
LLM 只看到过滤后的工具
```

---

## 为什么需要延迟加载？

- 几十个工具全部暴露给 LLM → Token 消耗大
- LLM 容易混淆，选错工具
- 延迟加载：只暴露当前需要的工具

---

👉 下一节：[10.6 最佳实践总结](10.6-最佳实践总结.md)
