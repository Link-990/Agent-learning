# 2.3 model 参数：多 Provider 支持

---

## Provider 字符串格式

```python
# 格式："provider:model_name"
agent = create_deep_agent(model="openai:gpt-5.5")
agent = create_deep_agent(model="anthropic:claude-sonnet-4-6")
agent = create_deep_agent(model="google_genai:gemini-3.5-flash")
agent = create_deep_agent(model="ollama:north-mini-code-1.0")
agent = create_deep_agent(model="baseten:zai-org/GLM-5.2")
agent = create_deep_agent(model="fireworks:accounts/fireworks/models/glm-5p2")
agent = create_deep_agent(model="openrouter:z-ai/glm-5.2")
```

---

## 主 Agent 和子 Agent 用不同模型

```python
agent = create_deep_agent(
    model="anthropic:claude-sonnet-4-6",  # 主 Agent 用 Claude
    subagents=[
        {
            "name": "researcher",
            "description": "搜索",
            "system_prompt": "你是研究员",
            "model": "openai:gpt-5.5",  # 子 Agent 用 GPT
        },
        {
            "name": "coder",
            "description": "编程",
            "system_prompt": "你是程序员",
            "model": "ollama:north-mini-code-1.0",  # 子 Agent 用本地模型
        },
    ],
)
```

---

👉 下一节：[2.4 system_prompt](2.4-system-prompt.md)
