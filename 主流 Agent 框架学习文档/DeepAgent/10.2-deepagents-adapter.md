# 10.2 deepagents_adapter.py：核心集成层

`deepagents_adapter.py` 是 Kami Code 与 Deep Agents 之间的桥梁——封装了 Deep Agent 的流式接口。

---

## 核心代码

```python
class DeepAgentsAdapter:
    def __init__(self, agent, model_profile):
        self._agent = agent
        self.model_profile = model_profile
    
    def stream_events(self, payload):
        """流式事件迭代器"""
        usage_accumulator = UsageAccumulator(model=self.model_profile.name)
        
        try:
            # 方式 1：messages stream mode
            for chunk in self._agent.stream(payload, stream_mode="messages"):
                usage_accumulator.observe(
                    usage_from_stream_chunk(chunk, model=self.model_profile.name)
                )
                yield from self._message_stream_chunk_to_events(chunk)
                yield from self.drain_tool_events()
        
        except TypeError:
            # 方式 2：降级到 state stream
            for chunk in self._agent.stream(payload):
                usage_accumulator.observe(
                    usage_from_stream_chunk(chunk, model=self.model_profile.name)
                )
                yield from self._state_stream_chunk_to_events(chunk)
                yield from self.drain_tool_events()
```

---

## 关键设计

1. **三级降级策略**：
   - 优先 `stream_events()`（适配器级）
   - 其次 `stream(stream_mode="messages")`（消息级）
   - 最后 `stream()`（状态级）

2. **Token 追踪**：在每个 chunk 中提取 usage_metadata

3. **统一事件接口**：不管底层用什么 stream mode，对外暴露统一事件

---

👉 下一节：[10.3 流式消息处理](10.3-流式消息处理.md)
