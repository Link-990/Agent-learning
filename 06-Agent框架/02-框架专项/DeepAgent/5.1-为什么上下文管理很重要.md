# 5.1 为什么上下文管理很重要？

---

## 问题

长对话 = 高 Token 消耗 = 高成本 + 慢速度。

一个复杂的编程任务可能需要 100+ 轮对话，每轮几千 Token。

## Deep Agents 的解决方案

1. **自动压缩**：对话太长时，自动摘要旧消息
2. **输出卸载**：工具返回的大数据，保存到文件，只留引用
3. **Memory 文件**：持久化的上下文，不占用对话窗口

---

👉 下一节：[5.2 自动上下文压缩](5.2-自动上下文压缩.md)
