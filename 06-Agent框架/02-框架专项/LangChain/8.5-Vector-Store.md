# 8.5 Vector Store

Vector Store 存储文档的向量，支持相似度检索。

---

## InMemoryVectorStore（开发用）

```python
from langchain_core.vectorstores import InMemoryVectorStore
from langchain_openai import OpenAIEmbeddings

embeddings = OpenAIEmbeddings()
vectorstore = InMemoryVectorStore(embeddings)

# 存入文档
vectorstore.add_documents(chunks)

# 检索
results = vectorstore.similarity_search("人工智能", k=3)
for doc in results:
    print(doc.page_content[:100])
```

---

## 常用 Vector Store

| Store | 适用场景 |
|-------|---------|
| InMemoryVectorStore | 开发测试 |
| Chroma | 小型项目 |
| FAISS | 大规模向量检索 |
| Pinecone | 云端托管 |
| Weaviate | 云端托管 |

---

👉 下一节：[8.6 Retriever](8.6-Retriever.md)
