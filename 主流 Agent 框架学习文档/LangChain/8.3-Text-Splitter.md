# 8.3 Text Splitter

Text Splitter 把长文档切分成小块——因为 LLM 的上下文窗口有限，向量数据库也需要小块检索。

---

## RecursiveCharacterTextSplitter（最常用）

```python
from langchain.text_splitter import RecursiveCharacterTextSplitter

splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,     # 每块最大 500 字符
    chunk_overlap=50,   # 块之间重叠 50 字符
)

chunks = splitter.split_documents(docs)
print(f"切分成 {len(chunks)} 个块")
```

---

## 参数说明

- **chunk_size**：每块的最大字符数
- **chunk_overlap**：相邻块的重叠字符数（防止切掉关键信息）

---

👉 下一节：[8.4 Embedding Model](8.4-Embedding-Model.md)
